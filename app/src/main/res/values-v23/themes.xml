<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Navyblue" parent="AppThemeBase">
        <item name="windowActionBar">false</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowSharedElementEnterTransition">@android:transition/move</item>
        <item name="android:windowSharedElementExitTransition">@android:transition/move</item>
        <item name="android:navigationBarColor">@color/color_navigation_bar</item>
        <item name="android:colorPrimary">@color/color_primary_light</item>
        <item name="android:colorPrimaryDark">@color/color_primary_dark</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowTranslucentStatus">false</item>

        <item name="android:textColorHint">@color/color_primary_light</item>
        <item name="colorAccent">@android:color/white</item>
        <item name="colorControlNormal">@color/dark_red</item>
        <item name="colorControlActivated">@color/green</item>
        <item name="colorControlHighlight">@color/gray</item>

        <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>

        <item name="textInputStyle">@style/TextInputLayoutTheme</item>
        <item name="ratingBarStyle">@style/StarRatingBar</item>
    </style>

    <style name="EditTextHint" parent="TextAppearance.AppCompat">
        <item name="android:textColorHint">@color/color_primary_light</item>
        <item name="colorAccent">@android:color/white</item>
        <item name="colorControlNormal">@color/dark_red</item>
        <item name="colorControlActivated">@color/green</item>
        <item name="colorControlHighlight">@color/gray</item>
    </style>

    <style name="TextInputLayoutTheme" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <!-- Color of the hint text inside the input field -->
        <item name="android:textColorHint">@color/white</item>

        <!-- The color of the label when it is collapsed and the text field is active -->
        <item name="hintTextColor">@color/white</item>

        <!-- Stroke color of the inactive input text layout -->
        <item name="colorOutline">@color/color_primary_light</item>
        <item name="textOutlineColor">@color/color_primary_light</item>

        <!-- Stroke color of the ACTIVE input text layout -->
        <item name="boxStrokeColor">@color/gray_light</item>
        <item name="boxStrokeWidth">1dp</item>
    </style>

    <style name="StarRatingBar" parent="Theme.AppCompat">
        <item name="android:progressTint">@color/color_accent</item>
        <item name="android:progressBackgroundTint">@color/gray</item>
    </style>
</resources>